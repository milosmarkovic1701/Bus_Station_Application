package com.thesis.bus_station.repository;

import com.thesis.bus_station.model.users.CityBusRide;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CityBusRideRepository extends JpaRepository<CityBusRide,Long> {

}
